name: "Sample react CI Pipeline / Workflow"

on:
    push:
        branches:
            - main
            - master

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
            - name : "Stepup Node.js"
              uses: actions/setup-node@v4
              with:
                node-version: 21