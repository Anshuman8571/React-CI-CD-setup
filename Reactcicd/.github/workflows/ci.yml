name: "Sample react CI Pipeline / Workflow"

on:
    push:
        branches:
            - main
            - master
            - origin

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
            - name : "Setup Node JS"
              uses: actions/setup-node@v2
              with:
                node-version: '14'
